# IsolatedAppDomain

ShadowKit.Legacy.NUnit.IsolatedAppDomain is a helper Library that extends NUnit to run Tests in another AppDomain.

**Attention: ** This is a .NET Framework-only Functionality, as .NET Core / .NET does not supports AppDomains.